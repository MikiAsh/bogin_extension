{
  "name": "Bob One Click Login",
  "source": "index.html",
  "scripts": {
    "clean-dist": "rm -rf dist && mkdir dist",
    "build": "npm run clean-dist && parcel build manifest.json --no-cache --no-source-maps"
  },
  "devDependencies": {
    "@parcel/config-webextension": "^2.8.3",
    "parcel": "^2.8.3"
  }
}
